services:
  - type: web
    name: mariposa-api
    env: docker
    plan: free
    region: frankfurt
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    buildFilter:
      paths:
        - Dockerfile
        - requirements.txt
        - src/**
    envVars:
      - key: PORT
        value: 9000
